name: willshex
description: All the base classes usually used for calling willshex apis plus the storage library for local persistence.
version: 0.0.1
homepage: https://www.willshex.com
publish_to: "none"

environment:
  sdk: ">=2.18.3 <3.0.0"

dependencies:
  path: ^1.8.2
  http: ^1.1.2
  universal_file: ^1.0.0
  logging: ^1.1.0

dev_dependencies:
  test: ^1.25.8
